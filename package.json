{
  "name": "painel-faturamento",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Painel de pedidos de venda e disponibilidade de faturamento (ERP on-premise)",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run build:server && npm run build:web",
    "build:server": "tsc -p tsconfig.json",
    "build:web": "npm --prefix web install --no-audit --no-fund && npm --prefix web run build",
    "start": "node dist/src/main.js",
    "dev": "tsc -p tsconfig.json --watch",
    "dev:web": "npm --prefix web run dev",
    "test": "npm run build:server && node --test \"dist/tests/*.test.js\"",
    "mock": "node dist/tools/erpFalso.js"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3"
  }
}
